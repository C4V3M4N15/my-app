{
  "name": "setlist-hub",
  "private": true,
  "devDependencies": {
    "turbo": "^1.11.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
